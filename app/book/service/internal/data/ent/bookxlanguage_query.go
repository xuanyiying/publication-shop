// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xuanyiying/publication-shop/app/book/service/internal/data/ent/bookxlanguage"
	"github.com/xuanyiying/publication-shop/app/book/service/internal/data/ent/predicate"
)

// BookXLanguageQuery is the builder for querying BookXLanguage entities.
type BookXLanguageQuery struct {
	config
	ctx        *QueryContext
	order      []bookxlanguage.OrderOption
	inters     []Interceptor
	predicates []predicate.BookXLanguage
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BookXLanguageQuery builder.
func (bxq *BookXLanguageQuery) Where(ps ...predicate.BookXLanguage) *BookXLanguageQuery {
	bxq.predicates = append(bxq.predicates, ps...)
	return bxq
}

// Limit the number of records to be returned by this query.
func (bxq *BookXLanguageQuery) Limit(limit int) *BookXLanguageQuery {
	bxq.ctx.Limit = &limit
	return bxq
}

// Offset to start from.
func (bxq *BookXLanguageQuery) Offset(offset int) *BookXLanguageQuery {
	bxq.ctx.Offset = &offset
	return bxq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bxq *BookXLanguageQuery) Unique(unique bool) *BookXLanguageQuery {
	bxq.ctx.Unique = &unique
	return bxq
}

// Order specifies how the records should be ordered.
func (bxq *BookXLanguageQuery) Order(o ...bookxlanguage.OrderOption) *BookXLanguageQuery {
	bxq.order = append(bxq.order, o...)
	return bxq
}

// First returns the first BookXLanguage entity from the query.
// Returns a *NotFoundError when no BookXLanguage was found.
func (bxq *BookXLanguageQuery) First(ctx context.Context) (*BookXLanguage, error) {
	nodes, err := bxq.Limit(1).All(setContextOp(ctx, bxq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bookxlanguage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bxq *BookXLanguageQuery) FirstX(ctx context.Context) *BookXLanguage {
	node, err := bxq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BookXLanguage ID from the query.
// Returns a *NotFoundError when no BookXLanguage ID was found.
func (bxq *BookXLanguageQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bxq.Limit(1).IDs(setContextOp(ctx, bxq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bookxlanguage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bxq *BookXLanguageQuery) FirstIDX(ctx context.Context) int {
	id, err := bxq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BookXLanguage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BookXLanguage entity is found.
// Returns a *NotFoundError when no BookXLanguage entities are found.
func (bxq *BookXLanguageQuery) Only(ctx context.Context) (*BookXLanguage, error) {
	nodes, err := bxq.Limit(2).All(setContextOp(ctx, bxq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bookxlanguage.Label}
	default:
		return nil, &NotSingularError{bookxlanguage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bxq *BookXLanguageQuery) OnlyX(ctx context.Context) *BookXLanguage {
	node, err := bxq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BookXLanguage ID in the query.
// Returns a *NotSingularError when more than one BookXLanguage ID is found.
// Returns a *NotFoundError when no entities are found.
func (bxq *BookXLanguageQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bxq.Limit(2).IDs(setContextOp(ctx, bxq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bookxlanguage.Label}
	default:
		err = &NotSingularError{bookxlanguage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bxq *BookXLanguageQuery) OnlyIDX(ctx context.Context) int {
	id, err := bxq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BookXLanguages.
func (bxq *BookXLanguageQuery) All(ctx context.Context) ([]*BookXLanguage, error) {
	ctx = setContextOp(ctx, bxq.ctx, "All")
	if err := bxq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BookXLanguage, *BookXLanguageQuery]()
	return withInterceptors[[]*BookXLanguage](ctx, bxq, qr, bxq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bxq *BookXLanguageQuery) AllX(ctx context.Context) []*BookXLanguage {
	nodes, err := bxq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BookXLanguage IDs.
func (bxq *BookXLanguageQuery) IDs(ctx context.Context) (ids []int, err error) {
	if bxq.ctx.Unique == nil && bxq.path != nil {
		bxq.Unique(true)
	}
	ctx = setContextOp(ctx, bxq.ctx, "IDs")
	if err = bxq.Select(bookxlanguage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bxq *BookXLanguageQuery) IDsX(ctx context.Context) []int {
	ids, err := bxq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bxq *BookXLanguageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bxq.ctx, "Count")
	if err := bxq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bxq, querierCount[*BookXLanguageQuery](), bxq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bxq *BookXLanguageQuery) CountX(ctx context.Context) int {
	count, err := bxq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bxq *BookXLanguageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bxq.ctx, "Exist")
	switch _, err := bxq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bxq *BookXLanguageQuery) ExistX(ctx context.Context) bool {
	exist, err := bxq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BookXLanguageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bxq *BookXLanguageQuery) Clone() *BookXLanguageQuery {
	if bxq == nil {
		return nil
	}
	return &BookXLanguageQuery{
		config:     bxq.config,
		ctx:        bxq.ctx.Clone(),
		order:      append([]bookxlanguage.OrderOption{}, bxq.order...),
		inters:     append([]Interceptor{}, bxq.inters...),
		predicates: append([]predicate.BookXLanguage{}, bxq.predicates...),
		// clone intermediate query.
		sql:  bxq.sql.Clone(),
		path: bxq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		LanguageID int `json:"language_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BookXLanguage.Query().
//		GroupBy(bookxlanguage.FieldLanguageID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bxq *BookXLanguageQuery) GroupBy(field string, fields ...string) *BookXLanguageGroupBy {
	bxq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BookXLanguageGroupBy{build: bxq}
	grbuild.flds = &bxq.ctx.Fields
	grbuild.label = bookxlanguage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		LanguageID int `json:"language_id,omitempty"`
//	}
//
//	client.BookXLanguage.Query().
//		Select(bookxlanguage.FieldLanguageID).
//		Scan(ctx, &v)
func (bxq *BookXLanguageQuery) Select(fields ...string) *BookXLanguageSelect {
	bxq.ctx.Fields = append(bxq.ctx.Fields, fields...)
	sbuild := &BookXLanguageSelect{BookXLanguageQuery: bxq}
	sbuild.label = bookxlanguage.Label
	sbuild.flds, sbuild.scan = &bxq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BookXLanguageSelect configured with the given aggregations.
func (bxq *BookXLanguageQuery) Aggregate(fns ...AggregateFunc) *BookXLanguageSelect {
	return bxq.Select().Aggregate(fns...)
}

func (bxq *BookXLanguageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bxq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bxq); err != nil {
				return err
			}
		}
	}
	for _, f := range bxq.ctx.Fields {
		if !bookxlanguage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bxq.path != nil {
		prev, err := bxq.path(ctx)
		if err != nil {
			return err
		}
		bxq.sql = prev
	}
	return nil
}

func (bxq *BookXLanguageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BookXLanguage, error) {
	var (
		nodes = []*BookXLanguage{}
		_spec = bxq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BookXLanguage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BookXLanguage{config: bxq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bxq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bxq *BookXLanguageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bxq.querySpec()
	_spec.Node.Columns = bxq.ctx.Fields
	if len(bxq.ctx.Fields) > 0 {
		_spec.Unique = bxq.ctx.Unique != nil && *bxq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bxq.driver, _spec)
}

func (bxq *BookXLanguageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bookxlanguage.Table, bookxlanguage.Columns, sqlgraph.NewFieldSpec(bookxlanguage.FieldID, field.TypeInt))
	_spec.From = bxq.sql
	if unique := bxq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bxq.path != nil {
		_spec.Unique = true
	}
	if fields := bxq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bookxlanguage.FieldID)
		for i := range fields {
			if fields[i] != bookxlanguage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bxq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bxq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bxq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bxq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bxq *BookXLanguageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bxq.driver.Dialect())
	t1 := builder.Table(bookxlanguage.Table)
	columns := bxq.ctx.Fields
	if len(columns) == 0 {
		columns = bookxlanguage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bxq.sql != nil {
		selector = bxq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bxq.ctx.Unique != nil && *bxq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bxq.predicates {
		p(selector)
	}
	for _, p := range bxq.order {
		p(selector)
	}
	if offset := bxq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bxq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BookXLanguageGroupBy is the group-by builder for BookXLanguage entities.
type BookXLanguageGroupBy struct {
	selector
	build *BookXLanguageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bxgb *BookXLanguageGroupBy) Aggregate(fns ...AggregateFunc) *BookXLanguageGroupBy {
	bxgb.fns = append(bxgb.fns, fns...)
	return bxgb
}

// Scan applies the selector query and scans the result into the given value.
func (bxgb *BookXLanguageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bxgb.build.ctx, "GroupBy")
	if err := bxgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BookXLanguageQuery, *BookXLanguageGroupBy](ctx, bxgb.build, bxgb, bxgb.build.inters, v)
}

func (bxgb *BookXLanguageGroupBy) sqlScan(ctx context.Context, root *BookXLanguageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bxgb.fns))
	for _, fn := range bxgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bxgb.flds)+len(bxgb.fns))
		for _, f := range *bxgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bxgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bxgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BookXLanguageSelect is the builder for selecting fields of BookXLanguage entities.
type BookXLanguageSelect struct {
	*BookXLanguageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bxs *BookXLanguageSelect) Aggregate(fns ...AggregateFunc) *BookXLanguageSelect {
	bxs.fns = append(bxs.fns, fns...)
	return bxs
}

// Scan applies the selector query and scans the result into the given value.
func (bxs *BookXLanguageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bxs.ctx, "Select")
	if err := bxs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BookXLanguageQuery, *BookXLanguageSelect](ctx, bxs.BookXLanguageQuery, bxs, bxs.inters, v)
}

func (bxs *BookXLanguageSelect) sqlScan(ctx context.Context, root *BookXLanguageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bxs.fns))
	for _, fn := range bxs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bxs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bxs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
