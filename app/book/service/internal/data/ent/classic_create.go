// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xuanyiying/publication-shop/app/book/service/internal/data/ent/classic"
)

// ClassicCreate is the builder for creating a Classic entity.
type ClassicCreate struct {
	config
	mutation *ClassicMutation
	hooks    []Hook
}

// SetClassicName sets the "classic_name" field.
func (cc *ClassicCreate) SetClassicName(s string) *ClassicCreate {
	cc.mutation.SetClassicName(s)
	return cc
}

// SetNillableClassicName sets the "classic_name" field if the given value is not nil.
func (cc *ClassicCreate) SetNillableClassicName(s *string) *ClassicCreate {
	if s != nil {
		cc.SetClassicName(*s)
	}
	return cc
}

// SetID sets the "id" field.
func (cc *ClassicCreate) SetID(i int) *ClassicCreate {
	cc.mutation.SetID(i)
	return cc
}

// Mutation returns the ClassicMutation object of the builder.
func (cc *ClassicCreate) Mutation() *ClassicMutation {
	return cc.mutation
}

// Save creates the Classic in the database.
func (cc *ClassicCreate) Save(ctx context.Context) (*Classic, error) {
	return withHooks(ctx, cc.sqlSave, cc.mutation, cc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (cc *ClassicCreate) SaveX(ctx context.Context) *Classic {
	v, err := cc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cc *ClassicCreate) Exec(ctx context.Context) error {
	_, err := cc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cc *ClassicCreate) ExecX(ctx context.Context) {
	if err := cc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cc *ClassicCreate) check() error {
	return nil
}

func (cc *ClassicCreate) sqlSave(ctx context.Context) (*Classic, error) {
	if err := cc.check(); err != nil {
		return nil, err
	}
	_node, _spec := cc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	cc.mutation.id = &_node.ID
	cc.mutation.done = true
	return _node, nil
}

func (cc *ClassicCreate) createSpec() (*Classic, *sqlgraph.CreateSpec) {
	var (
		_node = &Classic{config: cc.config}
		_spec = sqlgraph.NewCreateSpec(classic.Table, sqlgraph.NewFieldSpec(classic.FieldID, field.TypeInt))
	)
	if id, ok := cc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := cc.mutation.ClassicName(); ok {
		_spec.SetField(classic.FieldClassicName, field.TypeString, value)
		_node.ClassicName = value
	}
	return _node, _spec
}

// ClassicCreateBulk is the builder for creating many Classic entities in bulk.
type ClassicCreateBulk struct {
	config
	err      error
	builders []*ClassicCreate
}

// Save creates the Classic entities in the database.
func (ccb *ClassicCreateBulk) Save(ctx context.Context) ([]*Classic, error) {
	if ccb.err != nil {
		return nil, ccb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ccb.builders))
	nodes := make([]*Classic, len(ccb.builders))
	mutators := make([]Mutator, len(ccb.builders))
	for i := range ccb.builders {
		func(i int, root context.Context) {
			builder := ccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ClassicMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ccb *ClassicCreateBulk) SaveX(ctx context.Context) []*Classic {
	v, err := ccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ccb *ClassicCreateBulk) Exec(ctx context.Context) error {
	_, err := ccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ccb *ClassicCreateBulk) ExecX(ctx context.Context) {
	if err := ccb.Exec(ctx); err != nil {
		panic(err)
	}
}
