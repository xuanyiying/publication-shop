// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/xuanyiying/publication-shop/app/book/service/internal/data/ent/bookdetail"
)

// BookDetail is the model entity for the BookDetail schema.
type BookDetail struct {
	config `json:"-"`
	// ID of the ent.
	// 详情ID
	ID int `json:"id,omitempty"`
	// 国际标准书号
	Isbn string `json:"isbn,omitempty"`
	// 详情
	DetailHTML string `json:"detail_html,omitempty"`
	// 详情
	DetailImg string `json:"detail_img,omitempty"`
	// 出版物ID
	BookID       int `json:"book_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BookDetail) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bookdetail.FieldID, bookdetail.FieldBookID:
			values[i] = new(sql.NullInt64)
		case bookdetail.FieldIsbn, bookdetail.FieldDetailHTML, bookdetail.FieldDetailImg:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BookDetail fields.
func (bd *BookDetail) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bookdetail.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			bd.ID = int(value.Int64)
		case bookdetail.FieldIsbn:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field isbn", values[i])
			} else if value.Valid {
				bd.Isbn = value.String
			}
		case bookdetail.FieldDetailHTML:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field detail_html", values[i])
			} else if value.Valid {
				bd.DetailHTML = value.String
			}
		case bookdetail.FieldDetailImg:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field detail_img", values[i])
			} else if value.Valid {
				bd.DetailImg = value.String
			}
		case bookdetail.FieldBookID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field book_id", values[i])
			} else if value.Valid {
				bd.BookID = int(value.Int64)
			}
		default:
			bd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BookDetail.
// This includes values selected through modifiers, order, etc.
func (bd *BookDetail) Value(name string) (ent.Value, error) {
	return bd.selectValues.Get(name)
}

// Update returns a builder for updating this BookDetail.
// Note that you need to call BookDetail.Unwrap() before calling this method if this BookDetail
// was returned from a transaction, and the transaction was committed or rolled back.
func (bd *BookDetail) Update() *BookDetailUpdateOne {
	return NewBookDetailClient(bd.config).UpdateOne(bd)
}

// Unwrap unwraps the BookDetail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bd *BookDetail) Unwrap() *BookDetail {
	_tx, ok := bd.config.driver.(*txDriver)
	if !ok {
		panic("ent: BookDetail is not a transactional entity")
	}
	bd.config.driver = _tx.drv
	return bd
}

// String implements the fmt.Stringer.
func (bd *BookDetail) String() string {
	var builder strings.Builder
	builder.WriteString("BookDetail(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bd.ID))
	builder.WriteString("isbn=")
	builder.WriteString(bd.Isbn)
	builder.WriteString(", ")
	builder.WriteString("detail_html=")
	builder.WriteString(bd.DetailHTML)
	builder.WriteString(", ")
	builder.WriteString("detail_img=")
	builder.WriteString(bd.DetailImg)
	builder.WriteString(", ")
	builder.WriteString("book_id=")
	builder.WriteString(fmt.Sprintf("%v", bd.BookID))
	builder.WriteByte(')')
	return builder.String()
}

// BookDetails is a parsable slice of BookDetail.
type BookDetails []*BookDetail
