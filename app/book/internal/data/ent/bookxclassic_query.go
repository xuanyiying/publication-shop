// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xuanyiying/publication-shop/app/book/internal/data/ent/bookxclassic"
	"github.com/xuanyiying/publication-shop/app/book/internal/data/ent/predicate"
)

// BookXClassicQuery is the builder for querying BookXClassic entities.
type BookXClassicQuery struct {
	config
	ctx        *QueryContext
	order      []bookxclassic.OrderOption
	inters     []Interceptor
	predicates []predicate.BookXClassic
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BookXClassicQuery builder.
func (bxq *BookXClassicQuery) Where(ps ...predicate.BookXClassic) *BookXClassicQuery {
	bxq.predicates = append(bxq.predicates, ps...)
	return bxq
}

// Limit the number of records to be returned by this query.
func (bxq *BookXClassicQuery) Limit(limit int) *BookXClassicQuery {
	bxq.ctx.Limit = &limit
	return bxq
}

// Offset to start from.
func (bxq *BookXClassicQuery) Offset(offset int) *BookXClassicQuery {
	bxq.ctx.Offset = &offset
	return bxq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bxq *BookXClassicQuery) Unique(unique bool) *BookXClassicQuery {
	bxq.ctx.Unique = &unique
	return bxq
}

// Order specifies how the records should be ordered.
func (bxq *BookXClassicQuery) Order(o ...bookxclassic.OrderOption) *BookXClassicQuery {
	bxq.order = append(bxq.order, o...)
	return bxq
}

// First returns the first BookXClassic entity from the query.
// Returns a *NotFoundError when no BookXClassic was found.
func (bxq *BookXClassicQuery) First(ctx context.Context) (*BookXClassic, error) {
	nodes, err := bxq.Limit(1).All(setContextOp(ctx, bxq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bookxclassic.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bxq *BookXClassicQuery) FirstX(ctx context.Context) *BookXClassic {
	node, err := bxq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BookXClassic ID from the query.
// Returns a *NotFoundError when no BookXClassic ID was found.
func (bxq *BookXClassicQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bxq.Limit(1).IDs(setContextOp(ctx, bxq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bookxclassic.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bxq *BookXClassicQuery) FirstIDX(ctx context.Context) int {
	id, err := bxq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BookXClassic entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BookXClassic entity is found.
// Returns a *NotFoundError when no BookXClassic entities are found.
func (bxq *BookXClassicQuery) Only(ctx context.Context) (*BookXClassic, error) {
	nodes, err := bxq.Limit(2).All(setContextOp(ctx, bxq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bookxclassic.Label}
	default:
		return nil, &NotSingularError{bookxclassic.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bxq *BookXClassicQuery) OnlyX(ctx context.Context) *BookXClassic {
	node, err := bxq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BookXClassic ID in the query.
// Returns a *NotSingularError when more than one BookXClassic ID is found.
// Returns a *NotFoundError when no entities are found.
func (bxq *BookXClassicQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bxq.Limit(2).IDs(setContextOp(ctx, bxq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bookxclassic.Label}
	default:
		err = &NotSingularError{bookxclassic.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bxq *BookXClassicQuery) OnlyIDX(ctx context.Context) int {
	id, err := bxq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BookXClassics.
func (bxq *BookXClassicQuery) All(ctx context.Context) ([]*BookXClassic, error) {
	ctx = setContextOp(ctx, bxq.ctx, "All")
	if err := bxq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BookXClassic, *BookXClassicQuery]()
	return withInterceptors[[]*BookXClassic](ctx, bxq, qr, bxq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bxq *BookXClassicQuery) AllX(ctx context.Context) []*BookXClassic {
	nodes, err := bxq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BookXClassic IDs.
func (bxq *BookXClassicQuery) IDs(ctx context.Context) (ids []int, err error) {
	if bxq.ctx.Unique == nil && bxq.path != nil {
		bxq.Unique(true)
	}
	ctx = setContextOp(ctx, bxq.ctx, "IDs")
	if err = bxq.Select(bookxclassic.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bxq *BookXClassicQuery) IDsX(ctx context.Context) []int {
	ids, err := bxq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bxq *BookXClassicQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bxq.ctx, "Count")
	if err := bxq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bxq, querierCount[*BookXClassicQuery](), bxq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bxq *BookXClassicQuery) CountX(ctx context.Context) int {
	count, err := bxq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bxq *BookXClassicQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bxq.ctx, "Exist")
	switch _, err := bxq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bxq *BookXClassicQuery) ExistX(ctx context.Context) bool {
	exist, err := bxq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BookXClassicQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bxq *BookXClassicQuery) Clone() *BookXClassicQuery {
	if bxq == nil {
		return nil
	}
	return &BookXClassicQuery{
		config:     bxq.config,
		ctx:        bxq.ctx.Clone(),
		order:      append([]bookxclassic.OrderOption{}, bxq.order...),
		inters:     append([]Interceptor{}, bxq.inters...),
		predicates: append([]predicate.BookXClassic{}, bxq.predicates...),
		// clone intermediate query.
		sql:  bxq.sql.Clone(),
		path: bxq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClassicID int `json:"classic_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BookXClassic.Query().
//		GroupBy(bookxclassic.FieldClassicID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bxq *BookXClassicQuery) GroupBy(field string, fields ...string) *BookXClassicGroupBy {
	bxq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BookXClassicGroupBy{build: bxq}
	grbuild.flds = &bxq.ctx.Fields
	grbuild.label = bookxclassic.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClassicID int `json:"classic_id,omitempty"`
//	}
//
//	client.BookXClassic.Query().
//		Select(bookxclassic.FieldClassicID).
//		Scan(ctx, &v)
func (bxq *BookXClassicQuery) Select(fields ...string) *BookXClassicSelect {
	bxq.ctx.Fields = append(bxq.ctx.Fields, fields...)
	sbuild := &BookXClassicSelect{BookXClassicQuery: bxq}
	sbuild.label = bookxclassic.Label
	sbuild.flds, sbuild.scan = &bxq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BookXClassicSelect configured with the given aggregations.
func (bxq *BookXClassicQuery) Aggregate(fns ...AggregateFunc) *BookXClassicSelect {
	return bxq.Select().Aggregate(fns...)
}

func (bxq *BookXClassicQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bxq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bxq); err != nil {
				return err
			}
		}
	}
	for _, f := range bxq.ctx.Fields {
		if !bookxclassic.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bxq.path != nil {
		prev, err := bxq.path(ctx)
		if err != nil {
			return err
		}
		bxq.sql = prev
	}
	return nil
}

func (bxq *BookXClassicQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BookXClassic, error) {
	var (
		nodes = []*BookXClassic{}
		_spec = bxq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BookXClassic).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BookXClassic{config: bxq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bxq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bxq *BookXClassicQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bxq.querySpec()
	_spec.Node.Columns = bxq.ctx.Fields
	if len(bxq.ctx.Fields) > 0 {
		_spec.Unique = bxq.ctx.Unique != nil && *bxq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bxq.driver, _spec)
}

func (bxq *BookXClassicQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bookxclassic.Table, bookxclassic.Columns, sqlgraph.NewFieldSpec(bookxclassic.FieldID, field.TypeInt))
	_spec.From = bxq.sql
	if unique := bxq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bxq.path != nil {
		_spec.Unique = true
	}
	if fields := bxq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bookxclassic.FieldID)
		for i := range fields {
			if fields[i] != bookxclassic.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bxq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bxq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bxq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bxq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bxq *BookXClassicQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bxq.driver.Dialect())
	t1 := builder.Table(bookxclassic.Table)
	columns := bxq.ctx.Fields
	if len(columns) == 0 {
		columns = bookxclassic.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bxq.sql != nil {
		selector = bxq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bxq.ctx.Unique != nil && *bxq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bxq.predicates {
		p(selector)
	}
	for _, p := range bxq.order {
		p(selector)
	}
	if offset := bxq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bxq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BookXClassicGroupBy is the group-by builder for BookXClassic entities.
type BookXClassicGroupBy struct {
	selector
	build *BookXClassicQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bxgb *BookXClassicGroupBy) Aggregate(fns ...AggregateFunc) *BookXClassicGroupBy {
	bxgb.fns = append(bxgb.fns, fns...)
	return bxgb
}

// Scan applies the selector query and scans the result into the given value.
func (bxgb *BookXClassicGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bxgb.build.ctx, "GroupBy")
	if err := bxgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BookXClassicQuery, *BookXClassicGroupBy](ctx, bxgb.build, bxgb, bxgb.build.inters, v)
}

func (bxgb *BookXClassicGroupBy) sqlScan(ctx context.Context, root *BookXClassicQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bxgb.fns))
	for _, fn := range bxgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bxgb.flds)+len(bxgb.fns))
		for _, f := range *bxgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bxgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bxgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BookXClassicSelect is the builder for selecting fields of BookXClassic entities.
type BookXClassicSelect struct {
	*BookXClassicQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bxs *BookXClassicSelect) Aggregate(fns ...AggregateFunc) *BookXClassicSelect {
	bxs.fns = append(bxs.fns, fns...)
	return bxs
}

// Scan applies the selector query and scans the result into the given value.
func (bxs *BookXClassicSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bxs.ctx, "Select")
	if err := bxs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BookXClassicQuery, *BookXClassicSelect](ctx, bxs.BookXClassicQuery, bxs, bxs.inters, v)
}

func (bxs *BookXClassicSelect) sqlScan(ctx context.Context, root *BookXClassicQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bxs.fns))
	for _, fn := range bxs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bxs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bxs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
