// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xuanyiying/publication-shop/app/book/internal/data/ent/bookdetail"
	"github.com/xuanyiying/publication-shop/app/book/internal/data/ent/predicate"
)

// BookDetailQuery is the builder for querying BookDetail entities.
type BookDetailQuery struct {
	config
	ctx        *QueryContext
	order      []bookdetail.OrderOption
	inters     []Interceptor
	predicates []predicate.BookDetail
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BookDetailQuery builder.
func (bdq *BookDetailQuery) Where(ps ...predicate.BookDetail) *BookDetailQuery {
	bdq.predicates = append(bdq.predicates, ps...)
	return bdq
}

// Limit the number of records to be returned by this query.
func (bdq *BookDetailQuery) Limit(limit int) *BookDetailQuery {
	bdq.ctx.Limit = &limit
	return bdq
}

// Offset to start from.
func (bdq *BookDetailQuery) Offset(offset int) *BookDetailQuery {
	bdq.ctx.Offset = &offset
	return bdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bdq *BookDetailQuery) Unique(unique bool) *BookDetailQuery {
	bdq.ctx.Unique = &unique
	return bdq
}

// Order specifies how the records should be ordered.
func (bdq *BookDetailQuery) Order(o ...bookdetail.OrderOption) *BookDetailQuery {
	bdq.order = append(bdq.order, o...)
	return bdq
}

// First returns the first BookDetail entity from the query.
// Returns a *NotFoundError when no BookDetail was found.
func (bdq *BookDetailQuery) First(ctx context.Context) (*BookDetail, error) {
	nodes, err := bdq.Limit(1).All(setContextOp(ctx, bdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bookdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bdq *BookDetailQuery) FirstX(ctx context.Context) *BookDetail {
	node, err := bdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BookDetail ID from the query.
// Returns a *NotFoundError when no BookDetail ID was found.
func (bdq *BookDetailQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bdq.Limit(1).IDs(setContextOp(ctx, bdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bookdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bdq *BookDetailQuery) FirstIDX(ctx context.Context) int {
	id, err := bdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BookDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BookDetail entity is found.
// Returns a *NotFoundError when no BookDetail entities are found.
func (bdq *BookDetailQuery) Only(ctx context.Context) (*BookDetail, error) {
	nodes, err := bdq.Limit(2).All(setContextOp(ctx, bdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bookdetail.Label}
	default:
		return nil, &NotSingularError{bookdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bdq *BookDetailQuery) OnlyX(ctx context.Context) *BookDetail {
	node, err := bdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BookDetail ID in the query.
// Returns a *NotSingularError when more than one BookDetail ID is found.
// Returns a *NotFoundError when no entities are found.
func (bdq *BookDetailQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bdq.Limit(2).IDs(setContextOp(ctx, bdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bookdetail.Label}
	default:
		err = &NotSingularError{bookdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bdq *BookDetailQuery) OnlyIDX(ctx context.Context) int {
	id, err := bdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BookDetails.
func (bdq *BookDetailQuery) All(ctx context.Context) ([]*BookDetail, error) {
	ctx = setContextOp(ctx, bdq.ctx, "All")
	if err := bdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BookDetail, *BookDetailQuery]()
	return withInterceptors[[]*BookDetail](ctx, bdq, qr, bdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bdq *BookDetailQuery) AllX(ctx context.Context) []*BookDetail {
	nodes, err := bdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BookDetail IDs.
func (bdq *BookDetailQuery) IDs(ctx context.Context) (ids []int, err error) {
	if bdq.ctx.Unique == nil && bdq.path != nil {
		bdq.Unique(true)
	}
	ctx = setContextOp(ctx, bdq.ctx, "IDs")
	if err = bdq.Select(bookdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bdq *BookDetailQuery) IDsX(ctx context.Context) []int {
	ids, err := bdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bdq *BookDetailQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bdq.ctx, "Count")
	if err := bdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bdq, querierCount[*BookDetailQuery](), bdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bdq *BookDetailQuery) CountX(ctx context.Context) int {
	count, err := bdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bdq *BookDetailQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bdq.ctx, "Exist")
	switch _, err := bdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bdq *BookDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := bdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BookDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bdq *BookDetailQuery) Clone() *BookDetailQuery {
	if bdq == nil {
		return nil
	}
	return &BookDetailQuery{
		config:     bdq.config,
		ctx:        bdq.ctx.Clone(),
		order:      append([]bookdetail.OrderOption{}, bdq.order...),
		inters:     append([]Interceptor{}, bdq.inters...),
		predicates: append([]predicate.BookDetail{}, bdq.predicates...),
		// clone intermediate query.
		sql:  bdq.sql.Clone(),
		path: bdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Isbn string `json:"isbn,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BookDetail.Query().
//		GroupBy(bookdetail.FieldIsbn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bdq *BookDetailQuery) GroupBy(field string, fields ...string) *BookDetailGroupBy {
	bdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BookDetailGroupBy{build: bdq}
	grbuild.flds = &bdq.ctx.Fields
	grbuild.label = bookdetail.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Isbn string `json:"isbn,omitempty"`
//	}
//
//	client.BookDetail.Query().
//		Select(bookdetail.FieldIsbn).
//		Scan(ctx, &v)
func (bdq *BookDetailQuery) Select(fields ...string) *BookDetailSelect {
	bdq.ctx.Fields = append(bdq.ctx.Fields, fields...)
	sbuild := &BookDetailSelect{BookDetailQuery: bdq}
	sbuild.label = bookdetail.Label
	sbuild.flds, sbuild.scan = &bdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BookDetailSelect configured with the given aggregations.
func (bdq *BookDetailQuery) Aggregate(fns ...AggregateFunc) *BookDetailSelect {
	return bdq.Select().Aggregate(fns...)
}

func (bdq *BookDetailQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bdq); err != nil {
				return err
			}
		}
	}
	for _, f := range bdq.ctx.Fields {
		if !bookdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bdq.path != nil {
		prev, err := bdq.path(ctx)
		if err != nil {
			return err
		}
		bdq.sql = prev
	}
	return nil
}

func (bdq *BookDetailQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BookDetail, error) {
	var (
		nodes = []*BookDetail{}
		_spec = bdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BookDetail).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BookDetail{config: bdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bdq *BookDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bdq.querySpec()
	_spec.Node.Columns = bdq.ctx.Fields
	if len(bdq.ctx.Fields) > 0 {
		_spec.Unique = bdq.ctx.Unique != nil && *bdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bdq.driver, _spec)
}

func (bdq *BookDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bookdetail.Table, bookdetail.Columns, sqlgraph.NewFieldSpec(bookdetail.FieldID, field.TypeInt))
	_spec.From = bdq.sql
	if unique := bdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bdq.path != nil {
		_spec.Unique = true
	}
	if fields := bdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bookdetail.FieldID)
		for i := range fields {
			if fields[i] != bookdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bdq *BookDetailQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bdq.driver.Dialect())
	t1 := builder.Table(bookdetail.Table)
	columns := bdq.ctx.Fields
	if len(columns) == 0 {
		columns = bookdetail.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bdq.sql != nil {
		selector = bdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bdq.ctx.Unique != nil && *bdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bdq.predicates {
		p(selector)
	}
	for _, p := range bdq.order {
		p(selector)
	}
	if offset := bdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BookDetailGroupBy is the group-by builder for BookDetail entities.
type BookDetailGroupBy struct {
	selector
	build *BookDetailQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bdgb *BookDetailGroupBy) Aggregate(fns ...AggregateFunc) *BookDetailGroupBy {
	bdgb.fns = append(bdgb.fns, fns...)
	return bdgb
}

// Scan applies the selector query and scans the result into the given value.
func (bdgb *BookDetailGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bdgb.build.ctx, "GroupBy")
	if err := bdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BookDetailQuery, *BookDetailGroupBy](ctx, bdgb.build, bdgb, bdgb.build.inters, v)
}

func (bdgb *BookDetailGroupBy) sqlScan(ctx context.Context, root *BookDetailQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bdgb.fns))
	for _, fn := range bdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bdgb.flds)+len(bdgb.fns))
		for _, f := range *bdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BookDetailSelect is the builder for selecting fields of BookDetail entities.
type BookDetailSelect struct {
	*BookDetailQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bds *BookDetailSelect) Aggregate(fns ...AggregateFunc) *BookDetailSelect {
	bds.fns = append(bds.fns, fns...)
	return bds
}

// Scan applies the selector query and scans the result into the given value.
func (bds *BookDetailSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bds.ctx, "Select")
	if err := bds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BookDetailQuery, *BookDetailSelect](ctx, bds.BookDetailQuery, bds, bds.inters, v)
}

func (bds *BookDetailSelect) sqlScan(ctx context.Context, root *BookDetailQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bds.fns))
	for _, fn := range bds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
