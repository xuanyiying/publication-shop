// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/xuanyiying/publication-shop/app/book/internal/data/ent/bookxlanguage"
)

// BookXLanguage is the model entity for the BookXLanguage schema.
type BookXLanguage struct {
	config `json:"-"`
	// ID of the ent.
	// 出版物语言关系ID
	ID int `json:"id,omitempty"`
	// 详情ID
	LanguageID int `json:"language_id,omitempty"`
	// 国际标准书号
	Isbn string `json:"isbn,omitempty"`
	// 详情
	LanguageName string `json:"language_name,omitempty"`
	// 出版物ID
	BookID       int `json:"book_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BookXLanguage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bookxlanguage.FieldID, bookxlanguage.FieldLanguageID, bookxlanguage.FieldBookID:
			values[i] = new(sql.NullInt64)
		case bookxlanguage.FieldIsbn, bookxlanguage.FieldLanguageName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BookXLanguage fields.
func (bx *BookXLanguage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bookxlanguage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			bx.ID = int(value.Int64)
		case bookxlanguage.FieldLanguageID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field language_id", values[i])
			} else if value.Valid {
				bx.LanguageID = int(value.Int64)
			}
		case bookxlanguage.FieldIsbn:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field isbn", values[i])
			} else if value.Valid {
				bx.Isbn = value.String
			}
		case bookxlanguage.FieldLanguageName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field language_name", values[i])
			} else if value.Valid {
				bx.LanguageName = value.String
			}
		case bookxlanguage.FieldBookID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field book_id", values[i])
			} else if value.Valid {
				bx.BookID = int(value.Int64)
			}
		default:
			bx.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BookXLanguage.
// This includes values selected through modifiers, order, etc.
func (bx *BookXLanguage) Value(name string) (ent.Value, error) {
	return bx.selectValues.Get(name)
}

// Update returns a builder for updating this BookXLanguage.
// Note that you need to call BookXLanguage.Unwrap() before calling this method if this BookXLanguage
// was returned from a transaction, and the transaction was committed or rolled back.
func (bx *BookXLanguage) Update() *BookXLanguageUpdateOne {
	return NewBookXLanguageClient(bx.config).UpdateOne(bx)
}

// Unwrap unwraps the BookXLanguage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bx *BookXLanguage) Unwrap() *BookXLanguage {
	_tx, ok := bx.config.driver.(*txDriver)
	if !ok {
		panic("ent: BookXLanguage is not a transactional entity")
	}
	bx.config.driver = _tx.drv
	return bx
}

// String implements the fmt.Stringer.
func (bx *BookXLanguage) String() string {
	var builder strings.Builder
	builder.WriteString("BookXLanguage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bx.ID))
	builder.WriteString("language_id=")
	builder.WriteString(fmt.Sprintf("%v", bx.LanguageID))
	builder.WriteString(", ")
	builder.WriteString("isbn=")
	builder.WriteString(bx.Isbn)
	builder.WriteString(", ")
	builder.WriteString("language_name=")
	builder.WriteString(bx.LanguageName)
	builder.WriteString(", ")
	builder.WriteString("book_id=")
	builder.WriteString(fmt.Sprintf("%v", bx.BookID))
	builder.WriteByte(')')
	return builder.String()
}

// BookXLanguages is a parsable slice of BookXLanguage.
type BookXLanguages []*BookXLanguage
