// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/publication-shop/app/catalog/ent/predicate"
	"github.com/publication-shop/app/catalog/ent/publicationdetail"
)

// PublicationDetailQuery is the builder for querying PublicationDetail entities.
type PublicationDetailQuery struct {
	config
	ctx        *QueryContext
	order      []publicationdetail.OrderOption
	inters     []Interceptor
	predicates []predicate.PublicationDetail
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PublicationDetailQuery builder.
func (pdq *PublicationDetailQuery) Where(ps ...predicate.PublicationDetail) *PublicationDetailQuery {
	pdq.predicates = append(pdq.predicates, ps...)
	return pdq
}

// Limit the number of records to be returned by this query.
func (pdq *PublicationDetailQuery) Limit(limit int) *PublicationDetailQuery {
	pdq.ctx.Limit = &limit
	return pdq
}

// Offset to start from.
func (pdq *PublicationDetailQuery) Offset(offset int) *PublicationDetailQuery {
	pdq.ctx.Offset = &offset
	return pdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pdq *PublicationDetailQuery) Unique(unique bool) *PublicationDetailQuery {
	pdq.ctx.Unique = &unique
	return pdq
}

// Order specifies how the records should be ordered.
func (pdq *PublicationDetailQuery) Order(o ...publicationdetail.OrderOption) *PublicationDetailQuery {
	pdq.order = append(pdq.order, o...)
	return pdq
}

// First returns the first PublicationDetail entity from the query.
// Returns a *NotFoundError when no PublicationDetail was found.
func (pdq *PublicationDetailQuery) First(ctx context.Context) (*PublicationDetail, error) {
	nodes, err := pdq.Limit(1).All(setContextOp(ctx, pdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{publicationdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pdq *PublicationDetailQuery) FirstX(ctx context.Context) *PublicationDetail {
	node, err := pdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PublicationDetail ID from the query.
// Returns a *NotFoundError when no PublicationDetail ID was found.
func (pdq *PublicationDetailQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pdq.Limit(1).IDs(setContextOp(ctx, pdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{publicationdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pdq *PublicationDetailQuery) FirstIDX(ctx context.Context) int {
	id, err := pdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PublicationDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PublicationDetail entity is found.
// Returns a *NotFoundError when no PublicationDetail entities are found.
func (pdq *PublicationDetailQuery) Only(ctx context.Context) (*PublicationDetail, error) {
	nodes, err := pdq.Limit(2).All(setContextOp(ctx, pdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{publicationdetail.Label}
	default:
		return nil, &NotSingularError{publicationdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pdq *PublicationDetailQuery) OnlyX(ctx context.Context) *PublicationDetail {
	node, err := pdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PublicationDetail ID in the query.
// Returns a *NotSingularError when more than one PublicationDetail ID is found.
// Returns a *NotFoundError when no entities are found.
func (pdq *PublicationDetailQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pdq.Limit(2).IDs(setContextOp(ctx, pdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{publicationdetail.Label}
	default:
		err = &NotSingularError{publicationdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pdq *PublicationDetailQuery) OnlyIDX(ctx context.Context) int {
	id, err := pdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PublicationDetails.
func (pdq *PublicationDetailQuery) All(ctx context.Context) ([]*PublicationDetail, error) {
	ctx = setContextOp(ctx, pdq.ctx, "All")
	if err := pdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PublicationDetail, *PublicationDetailQuery]()
	return withInterceptors[[]*PublicationDetail](ctx, pdq, qr, pdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pdq *PublicationDetailQuery) AllX(ctx context.Context) []*PublicationDetail {
	nodes, err := pdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PublicationDetail IDs.
func (pdq *PublicationDetailQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pdq.ctx.Unique == nil && pdq.path != nil {
		pdq.Unique(true)
	}
	ctx = setContextOp(ctx, pdq.ctx, "IDs")
	if err = pdq.Select(publicationdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pdq *PublicationDetailQuery) IDsX(ctx context.Context) []int {
	ids, err := pdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pdq *PublicationDetailQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pdq.ctx, "Count")
	if err := pdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pdq, querierCount[*PublicationDetailQuery](), pdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pdq *PublicationDetailQuery) CountX(ctx context.Context) int {
	count, err := pdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pdq *PublicationDetailQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pdq.ctx, "Exist")
	switch _, err := pdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pdq *PublicationDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := pdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PublicationDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pdq *PublicationDetailQuery) Clone() *PublicationDetailQuery {
	if pdq == nil {
		return nil
	}
	return &PublicationDetailQuery{
		config:     pdq.config,
		ctx:        pdq.ctx.Clone(),
		order:      append([]publicationdetail.OrderOption{}, pdq.order...),
		inters:     append([]Interceptor{}, pdq.inters...),
		predicates: append([]predicate.PublicationDetail{}, pdq.predicates...),
		// clone intermediate query.
		sql:  pdq.sql.Clone(),
		path: pdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Isbn string `json:"isbn,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PublicationDetail.Query().
//		GroupBy(publicationdetail.FieldIsbn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pdq *PublicationDetailQuery) GroupBy(field string, fields ...string) *PublicationDetailGroupBy {
	pdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PublicationDetailGroupBy{build: pdq}
	grbuild.flds = &pdq.ctx.Fields
	grbuild.label = publicationdetail.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Isbn string `json:"isbn,omitempty"`
//	}
//
//	client.PublicationDetail.Query().
//		Select(publicationdetail.FieldIsbn).
//		Scan(ctx, &v)
func (pdq *PublicationDetailQuery) Select(fields ...string) *PublicationDetailSelect {
	pdq.ctx.Fields = append(pdq.ctx.Fields, fields...)
	sbuild := &PublicationDetailSelect{PublicationDetailQuery: pdq}
	sbuild.label = publicationdetail.Label
	sbuild.flds, sbuild.scan = &pdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PublicationDetailSelect configured with the given aggregations.
func (pdq *PublicationDetailQuery) Aggregate(fns ...AggregateFunc) *PublicationDetailSelect {
	return pdq.Select().Aggregate(fns...)
}

func (pdq *PublicationDetailQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pdq); err != nil {
				return err
			}
		}
	}
	for _, f := range pdq.ctx.Fields {
		if !publicationdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pdq.path != nil {
		prev, err := pdq.path(ctx)
		if err != nil {
			return err
		}
		pdq.sql = prev
	}
	return nil
}

func (pdq *PublicationDetailQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PublicationDetail, error) {
	var (
		nodes = []*PublicationDetail{}
		_spec = pdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PublicationDetail).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PublicationDetail{config: pdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pdq *PublicationDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pdq.querySpec()
	_spec.Node.Columns = pdq.ctx.Fields
	if len(pdq.ctx.Fields) > 0 {
		_spec.Unique = pdq.ctx.Unique != nil && *pdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pdq.driver, _spec)
}

func (pdq *PublicationDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(publicationdetail.Table, publicationdetail.Columns, sqlgraph.NewFieldSpec(publicationdetail.FieldID, field.TypeInt))
	_spec.From = pdq.sql
	if unique := pdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pdq.path != nil {
		_spec.Unique = true
	}
	if fields := pdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, publicationdetail.FieldID)
		for i := range fields {
			if fields[i] != publicationdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pdq *PublicationDetailQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pdq.driver.Dialect())
	t1 := builder.Table(publicationdetail.Table)
	columns := pdq.ctx.Fields
	if len(columns) == 0 {
		columns = publicationdetail.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pdq.sql != nil {
		selector = pdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pdq.ctx.Unique != nil && *pdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pdq.predicates {
		p(selector)
	}
	for _, p := range pdq.order {
		p(selector)
	}
	if offset := pdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PublicationDetailGroupBy is the group-by builder for PublicationDetail entities.
type PublicationDetailGroupBy struct {
	selector
	build *PublicationDetailQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pdgb *PublicationDetailGroupBy) Aggregate(fns ...AggregateFunc) *PublicationDetailGroupBy {
	pdgb.fns = append(pdgb.fns, fns...)
	return pdgb
}

// Scan applies the selector query and scans the result into the given value.
func (pdgb *PublicationDetailGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pdgb.build.ctx, "GroupBy")
	if err := pdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PublicationDetailQuery, *PublicationDetailGroupBy](ctx, pdgb.build, pdgb, pdgb.build.inters, v)
}

func (pdgb *PublicationDetailGroupBy) sqlScan(ctx context.Context, root *PublicationDetailQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pdgb.fns))
	for _, fn := range pdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pdgb.flds)+len(pdgb.fns))
		for _, f := range *pdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PublicationDetailSelect is the builder for selecting fields of PublicationDetail entities.
type PublicationDetailSelect struct {
	*PublicationDetailQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pds *PublicationDetailSelect) Aggregate(fns ...AggregateFunc) *PublicationDetailSelect {
	pds.fns = append(pds.fns, fns...)
	return pds
}

// Scan applies the selector query and scans the result into the given value.
func (pds *PublicationDetailSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pds.ctx, "Select")
	if err := pds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PublicationDetailQuery, *PublicationDetailSelect](ctx, pds.PublicationDetailQuery, pds, pds.inters, v)
}

func (pds *PublicationDetailSelect) sqlScan(ctx context.Context, root *PublicationDetailQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pds.fns))
	for _, fn := range pds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
