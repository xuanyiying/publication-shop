// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xuanyiying/publication-shop/app/catalog/ent/predicate"
	"github.com/xuanyiying/publication-shop/app/catalog/ent/publicationorg"
)

// PublicationOrgQuery is the builder for querying PublicationOrg entities.
type PublicationOrgQuery struct {
	config
	ctx        *QueryContext
	order      []publicationorg.OrderOption
	inters     []Interceptor
	predicates []predicate.PublicationOrg
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PublicationOrgQuery builder.
func (poq *PublicationOrgQuery) Where(ps ...predicate.PublicationOrg) *PublicationOrgQuery {
	poq.predicates = append(poq.predicates, ps...)
	return poq
}

// Limit the number of records to be returned by this query.
func (poq *PublicationOrgQuery) Limit(limit int) *PublicationOrgQuery {
	poq.ctx.Limit = &limit
	return poq
}

// Offset to start from.
func (poq *PublicationOrgQuery) Offset(offset int) *PublicationOrgQuery {
	poq.ctx.Offset = &offset
	return poq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (poq *PublicationOrgQuery) Unique(unique bool) *PublicationOrgQuery {
	poq.ctx.Unique = &unique
	return poq
}

// Order specifies how the records should be ordered.
func (poq *PublicationOrgQuery) Order(o ...publicationorg.OrderOption) *PublicationOrgQuery {
	poq.order = append(poq.order, o...)
	return poq
}

// First returns the first PublicationOrg entity from the query.
// Returns a *NotFoundError when no PublicationOrg was found.
func (poq *PublicationOrgQuery) First(ctx context.Context) (*PublicationOrg, error) {
	nodes, err := poq.Limit(1).All(setContextOp(ctx, poq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{publicationorg.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (poq *PublicationOrgQuery) FirstX(ctx context.Context) *PublicationOrg {
	node, err := poq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PublicationOrg ID from the query.
// Returns a *NotFoundError when no PublicationOrg ID was found.
func (poq *PublicationOrgQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = poq.Limit(1).IDs(setContextOp(ctx, poq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{publicationorg.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (poq *PublicationOrgQuery) FirstIDX(ctx context.Context) int {
	id, err := poq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PublicationOrg entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PublicationOrg entity is found.
// Returns a *NotFoundError when no PublicationOrg entities are found.
func (poq *PublicationOrgQuery) Only(ctx context.Context) (*PublicationOrg, error) {
	nodes, err := poq.Limit(2).All(setContextOp(ctx, poq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{publicationorg.Label}
	default:
		return nil, &NotSingularError{publicationorg.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (poq *PublicationOrgQuery) OnlyX(ctx context.Context) *PublicationOrg {
	node, err := poq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PublicationOrg ID in the query.
// Returns a *NotSingularError when more than one PublicationOrg ID is found.
// Returns a *NotFoundError when no entities are found.
func (poq *PublicationOrgQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = poq.Limit(2).IDs(setContextOp(ctx, poq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{publicationorg.Label}
	default:
		err = &NotSingularError{publicationorg.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (poq *PublicationOrgQuery) OnlyIDX(ctx context.Context) int {
	id, err := poq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PublicationOrgs.
func (poq *PublicationOrgQuery) All(ctx context.Context) ([]*PublicationOrg, error) {
	ctx = setContextOp(ctx, poq.ctx, "All")
	if err := poq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PublicationOrg, *PublicationOrgQuery]()
	return withInterceptors[[]*PublicationOrg](ctx, poq, qr, poq.inters)
}

// AllX is like All, but panics if an error occurs.
func (poq *PublicationOrgQuery) AllX(ctx context.Context) []*PublicationOrg {
	nodes, err := poq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PublicationOrg IDs.
func (poq *PublicationOrgQuery) IDs(ctx context.Context) (ids []int, err error) {
	if poq.ctx.Unique == nil && poq.path != nil {
		poq.Unique(true)
	}
	ctx = setContextOp(ctx, poq.ctx, "IDs")
	if err = poq.Select(publicationorg.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (poq *PublicationOrgQuery) IDsX(ctx context.Context) []int {
	ids, err := poq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (poq *PublicationOrgQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, poq.ctx, "Count")
	if err := poq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, poq, querierCount[*PublicationOrgQuery](), poq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (poq *PublicationOrgQuery) CountX(ctx context.Context) int {
	count, err := poq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (poq *PublicationOrgQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, poq.ctx, "Exist")
	switch _, err := poq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (poq *PublicationOrgQuery) ExistX(ctx context.Context) bool {
	exist, err := poq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PublicationOrgQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (poq *PublicationOrgQuery) Clone() *PublicationOrgQuery {
	if poq == nil {
		return nil
	}
	return &PublicationOrgQuery{
		config:     poq.config,
		ctx:        poq.ctx.Clone(),
		order:      append([]publicationorg.OrderOption{}, poq.order...),
		inters:     append([]Interceptor{}, poq.inters...),
		predicates: append([]predicate.PublicationOrg{}, poq.predicates...),
		// clone intermediate query.
		sql:  poq.sql.Clone(),
		path: poq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OrgCode string `json:"org_code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PublicationOrg.Query().
//		GroupBy(publicationorg.FieldOrgCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (poq *PublicationOrgQuery) GroupBy(field string, fields ...string) *PublicationOrgGroupBy {
	poq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PublicationOrgGroupBy{build: poq}
	grbuild.flds = &poq.ctx.Fields
	grbuild.label = publicationorg.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OrgCode string `json:"org_code,omitempty"`
//	}
//
//	client.PublicationOrg.Query().
//		Select(publicationorg.FieldOrgCode).
//		Scan(ctx, &v)
func (poq *PublicationOrgQuery) Select(fields ...string) *PublicationOrgSelect {
	poq.ctx.Fields = append(poq.ctx.Fields, fields...)
	sbuild := &PublicationOrgSelect{PublicationOrgQuery: poq}
	sbuild.label = publicationorg.Label
	sbuild.flds, sbuild.scan = &poq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PublicationOrgSelect configured with the given aggregations.
func (poq *PublicationOrgQuery) Aggregate(fns ...AggregateFunc) *PublicationOrgSelect {
	return poq.Select().Aggregate(fns...)
}

func (poq *PublicationOrgQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range poq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, poq); err != nil {
				return err
			}
		}
	}
	for _, f := range poq.ctx.Fields {
		if !publicationorg.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if poq.path != nil {
		prev, err := poq.path(ctx)
		if err != nil {
			return err
		}
		poq.sql = prev
	}
	return nil
}

func (poq *PublicationOrgQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PublicationOrg, error) {
	var (
		nodes = []*PublicationOrg{}
		_spec = poq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PublicationOrg).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PublicationOrg{config: poq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, poq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (poq *PublicationOrgQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := poq.querySpec()
	_spec.Node.Columns = poq.ctx.Fields
	if len(poq.ctx.Fields) > 0 {
		_spec.Unique = poq.ctx.Unique != nil && *poq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, poq.driver, _spec)
}

func (poq *PublicationOrgQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(publicationorg.Table, publicationorg.Columns, sqlgraph.NewFieldSpec(publicationorg.FieldID, field.TypeInt))
	_spec.From = poq.sql
	if unique := poq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if poq.path != nil {
		_spec.Unique = true
	}
	if fields := poq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, publicationorg.FieldID)
		for i := range fields {
			if fields[i] != publicationorg.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := poq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := poq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := poq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := poq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (poq *PublicationOrgQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(poq.driver.Dialect())
	t1 := builder.Table(publicationorg.Table)
	columns := poq.ctx.Fields
	if len(columns) == 0 {
		columns = publicationorg.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if poq.sql != nil {
		selector = poq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if poq.ctx.Unique != nil && *poq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range poq.predicates {
		p(selector)
	}
	for _, p := range poq.order {
		p(selector)
	}
	if offset := poq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := poq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PublicationOrgGroupBy is the group-by builder for PublicationOrg entities.
type PublicationOrgGroupBy struct {
	selector
	build *PublicationOrgQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pogb *PublicationOrgGroupBy) Aggregate(fns ...AggregateFunc) *PublicationOrgGroupBy {
	pogb.fns = append(pogb.fns, fns...)
	return pogb
}

// Scan applies the selector query and scans the result into the given value.
func (pogb *PublicationOrgGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pogb.build.ctx, "GroupBy")
	if err := pogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PublicationOrgQuery, *PublicationOrgGroupBy](ctx, pogb.build, pogb, pogb.build.inters, v)
}

func (pogb *PublicationOrgGroupBy) sqlScan(ctx context.Context, root *PublicationOrgQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pogb.fns))
	for _, fn := range pogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pogb.flds)+len(pogb.fns))
		for _, f := range *pogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PublicationOrgSelect is the builder for selecting fields of PublicationOrg entities.
type PublicationOrgSelect struct {
	*PublicationOrgQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pos *PublicationOrgSelect) Aggregate(fns ...AggregateFunc) *PublicationOrgSelect {
	pos.fns = append(pos.fns, fns...)
	return pos
}

// Scan applies the selector query and scans the result into the given value.
func (pos *PublicationOrgSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pos.ctx, "Select")
	if err := pos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PublicationOrgQuery, *PublicationOrgSelect](ctx, pos.PublicationOrgQuery, pos, pos.inters, v)
}

func (pos *PublicationOrgSelect) sqlScan(ctx context.Context, root *PublicationOrgQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pos.fns))
	for _, fn := range pos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
