// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/xuanyiying/publication-shop/app/catalog/ent/publicationstock"
)

// PublicationStock is the model entity for the PublicationStock schema.
type PublicationStock struct {
	config `json:"-"`
	// ID of the ent.
	// 库存Id
	ID int `json:"id,omitempty"`
	// 国际标准书号
	Isbn string `json:"isbn,omitempty"`
	// 数量
	Quantity int `json:"quantity,omitempty"`
	// 出版物ID
	PublicationID int `json:"publication_id,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PublicationStock) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case publicationstock.FieldID, publicationstock.FieldQuantity, publicationstock.FieldPublicationID:
			values[i] = new(sql.NullInt64)
		case publicationstock.FieldIsbn:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PublicationStock fields.
func (ps *PublicationStock) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case publicationstock.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ps.ID = int(value.Int64)
		case publicationstock.FieldIsbn:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field isbn", values[i])
			} else if value.Valid {
				ps.Isbn = value.String
			}
		case publicationstock.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				ps.Quantity = int(value.Int64)
			}
		case publicationstock.FieldPublicationID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field publication_id", values[i])
			} else if value.Valid {
				ps.PublicationID = int(value.Int64)
			}
		default:
			ps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PublicationStock.
// This includes values selected through modifiers, order, etc.
func (ps *PublicationStock) Value(name string) (ent.Value, error) {
	return ps.selectValues.Get(name)
}

// Update returns a builder for updating this PublicationStock.
// Note that you need to call PublicationStock.Unwrap() before calling this method if this PublicationStock
// was returned from a transaction, and the transaction was committed or rolled back.
func (ps *PublicationStock) Update() *PublicationStockUpdateOne {
	return NewPublicationStockClient(ps.config).UpdateOne(ps)
}

// Unwrap unwraps the PublicationStock entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ps *PublicationStock) Unwrap() *PublicationStock {
	_tx, ok := ps.config.driver.(*txDriver)
	if !ok {
		panic("ent: PublicationStock is not a transactional entity")
	}
	ps.config.driver = _tx.drv
	return ps
}

// String implements the fmt.Stringer.
func (ps *PublicationStock) String() string {
	var builder strings.Builder
	builder.WriteString("PublicationStock(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ps.ID))
	builder.WriteString("isbn=")
	builder.WriteString(ps.Isbn)
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", ps.Quantity))
	builder.WriteString(", ")
	builder.WriteString("publication_id=")
	builder.WriteString(fmt.Sprintf("%v", ps.PublicationID))
	builder.WriteByte(')')
	return builder.String()
}

// PublicationStocks is a parsable slice of PublicationStock.
type PublicationStocks []*PublicationStock
