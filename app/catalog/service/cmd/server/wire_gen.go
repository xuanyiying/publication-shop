// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/biz"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/conf"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/data"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/server"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/service"
	"go.opentelemetry.io/otel/sdk/trace"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(confServer *conf.Server, registry *conf.Registry, confData *conf.Data, logger log.Logger, tracerProvider *trace.TracerProvider) (*kratos.App, func(), error) {
	client := data.NewEntClient(confData, logger)
	dataData, cleanup, err := data.NewData(client, logger)
	if err != nil {
		return nil, nil, err
	}
	PublicationRepo := data.NewPublicationRepo(dataData, logger)
	PublicationUseCase := biz.NewPublicationUseCase(PublicationRepo, logger)
	catalogService := service.NewCatalogService(PublicationUseCase, logger)
	grpcServer := server.NewGRPCServer(confServer, logger, tracerProvider, catalogService)
	registrar := server.NewRegistrar(registry)
	app := newApp(logger, grpcServer, registrar)
	return app, func() {
		cleanup()
	}, nil
}
