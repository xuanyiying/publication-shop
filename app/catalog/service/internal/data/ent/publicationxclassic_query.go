// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/data/ent/predicate"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/data/ent/publicationxclassic"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PublicationXClassicQuery is the builder for querying PublicationXClassic entities.
type PublicationXClassicQuery struct {
	config
	ctx        *QueryContext
	order      []publicationxclassic.OrderOption
	inters     []Interceptor
	predicates []predicate.PublicationXClassic
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PublicationXClassicQuery builder.
func (pxq *PublicationXClassicQuery) Where(ps ...predicate.PublicationXClassic) *PublicationXClassicQuery {
	pxq.predicates = append(pxq.predicates, ps...)
	return pxq
}

// Limit the number of records to be returned by this query.
func (pxq *PublicationXClassicQuery) Limit(limit int) *PublicationXClassicQuery {
	pxq.ctx.Limit = &limit
	return pxq
}

// Offset to start from.
func (pxq *PublicationXClassicQuery) Offset(offset int) *PublicationXClassicQuery {
	pxq.ctx.Offset = &offset
	return pxq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pxq *PublicationXClassicQuery) Unique(unique bool) *PublicationXClassicQuery {
	pxq.ctx.Unique = &unique
	return pxq
}

// Order specifies how the records should be ordered.
func (pxq *PublicationXClassicQuery) Order(o ...publicationxclassic.OrderOption) *PublicationXClassicQuery {
	pxq.order = append(pxq.order, o...)
	return pxq
}

// First returns the first PublicationXClassic entity from the query.
// Returns a *NotFoundError when no PublicationXClassic was found.
func (pxq *PublicationXClassicQuery) First(ctx context.Context) (*PublicationXClassic, error) {
	nodes, err := pxq.Limit(1).All(setContextOp(ctx, pxq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{publicationxclassic.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) FirstX(ctx context.Context) *PublicationXClassic {
	node, err := pxq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PublicationXClassic ID from the query.
// Returns a *NotFoundError when no PublicationXClassic ID was found.
func (pxq *PublicationXClassicQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pxq.Limit(1).IDs(setContextOp(ctx, pxq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{publicationxclassic.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) FirstIDX(ctx context.Context) int {
	id, err := pxq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PublicationXClassic entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PublicationXClassic entity is found.
// Returns a *NotFoundError when no PublicationXClassic entities are found.
func (pxq *PublicationXClassicQuery) Only(ctx context.Context) (*PublicationXClassic, error) {
	nodes, err := pxq.Limit(2).All(setContextOp(ctx, pxq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{publicationxclassic.Label}
	default:
		return nil, &NotSingularError{publicationxclassic.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) OnlyX(ctx context.Context) *PublicationXClassic {
	node, err := pxq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PublicationXClassic ID in the query.
// Returns a *NotSingularError when more than one PublicationXClassic ID is found.
// Returns a *NotFoundError when no entities are found.
func (pxq *PublicationXClassicQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pxq.Limit(2).IDs(setContextOp(ctx, pxq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{publicationxclassic.Label}
	default:
		err = &NotSingularError{publicationxclassic.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) OnlyIDX(ctx context.Context) int {
	id, err := pxq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PublicationXClassics.
func (pxq *PublicationXClassicQuery) All(ctx context.Context) ([]*PublicationXClassic, error) {
	ctx = setContextOp(ctx, pxq.ctx, "All")
	if err := pxq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PublicationXClassic, *PublicationXClassicQuery]()
	return withInterceptors[[]*PublicationXClassic](ctx, pxq, qr, pxq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) AllX(ctx context.Context) []*PublicationXClassic {
	nodes, err := pxq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PublicationXClassic IDs.
func (pxq *PublicationXClassicQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pxq.ctx.Unique == nil && pxq.path != nil {
		pxq.Unique(true)
	}
	ctx = setContextOp(ctx, pxq.ctx, "IDs")
	if err = pxq.Select(publicationxclassic.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) IDsX(ctx context.Context) []int {
	ids, err := pxq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pxq *PublicationXClassicQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pxq.ctx, "Count")
	if err := pxq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pxq, querierCount[*PublicationXClassicQuery](), pxq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) CountX(ctx context.Context) int {
	count, err := pxq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pxq *PublicationXClassicQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pxq.ctx, "Exist")
	switch _, err := pxq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pxq *PublicationXClassicQuery) ExistX(ctx context.Context) bool {
	exist, err := pxq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PublicationXClassicQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pxq *PublicationXClassicQuery) Clone() *PublicationXClassicQuery {
	if pxq == nil {
		return nil
	}
	return &PublicationXClassicQuery{
		config:     pxq.config,
		ctx:        pxq.ctx.Clone(),
		order:      append([]publicationxclassic.OrderOption{}, pxq.order...),
		inters:     append([]Interceptor{}, pxq.inters...),
		predicates: append([]predicate.PublicationXClassic{}, pxq.predicates...),
		// clone intermediate query.
		sql:  pxq.sql.Clone(),
		path: pxq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClassicID int `json:"classic_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PublicationXClassic.Query().
//		GroupBy(publicationxclassic.FieldClassicID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pxq *PublicationXClassicQuery) GroupBy(field string, fields ...string) *PublicationXClassicGroupBy {
	pxq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PublicationXClassicGroupBy{build: pxq}
	grbuild.flds = &pxq.ctx.Fields
	grbuild.label = publicationxclassic.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClassicID int `json:"classic_id,omitempty"`
//	}
//
//	client.PublicationXClassic.Query().
//		Select(publicationxclassic.FieldClassicID).
//		Scan(ctx, &v)
func (pxq *PublicationXClassicQuery) Select(fields ...string) *PublicationXClassicSelect {
	pxq.ctx.Fields = append(pxq.ctx.Fields, fields...)
	sbuild := &PublicationXClassicSelect{PublicationXClassicQuery: pxq}
	sbuild.label = publicationxclassic.Label
	sbuild.flds, sbuild.scan = &pxq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PublicationXClassicSelect configured with the given aggregations.
func (pxq *PublicationXClassicQuery) Aggregate(fns ...AggregateFunc) *PublicationXClassicSelect {
	return pxq.Select().Aggregate(fns...)
}

func (pxq *PublicationXClassicQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pxq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pxq); err != nil {
				return err
			}
		}
	}
	for _, f := range pxq.ctx.Fields {
		if !publicationxclassic.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pxq.path != nil {
		prev, err := pxq.path(ctx)
		if err != nil {
			return err
		}
		pxq.sql = prev
	}
	return nil
}

func (pxq *PublicationXClassicQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PublicationXClassic, error) {
	var (
		nodes = []*PublicationXClassic{}
		_spec = pxq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PublicationXClassic).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PublicationXClassic{config: pxq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pxq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pxq *PublicationXClassicQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pxq.querySpec()
	_spec.Node.Columns = pxq.ctx.Fields
	if len(pxq.ctx.Fields) > 0 {
		_spec.Unique = pxq.ctx.Unique != nil && *pxq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pxq.driver, _spec)
}

func (pxq *PublicationXClassicQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(publicationxclassic.Table, publicationxclassic.Columns, sqlgraph.NewFieldSpec(publicationxclassic.FieldID, field.TypeInt))
	_spec.From = pxq.sql
	if unique := pxq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pxq.path != nil {
		_spec.Unique = true
	}
	if fields := pxq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, publicationxclassic.FieldID)
		for i := range fields {
			if fields[i] != publicationxclassic.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pxq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pxq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pxq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pxq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pxq *PublicationXClassicQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pxq.driver.Dialect())
	t1 := builder.Table(publicationxclassic.Table)
	columns := pxq.ctx.Fields
	if len(columns) == 0 {
		columns = publicationxclassic.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pxq.sql != nil {
		selector = pxq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pxq.ctx.Unique != nil && *pxq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pxq.predicates {
		p(selector)
	}
	for _, p := range pxq.order {
		p(selector)
	}
	if offset := pxq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pxq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PublicationXClassicGroupBy is the group-by builder for PublicationXClassic entities.
type PublicationXClassicGroupBy struct {
	selector
	build *PublicationXClassicQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pxgb *PublicationXClassicGroupBy) Aggregate(fns ...AggregateFunc) *PublicationXClassicGroupBy {
	pxgb.fns = append(pxgb.fns, fns...)
	return pxgb
}

// Scan applies the selector query and scans the result into the given value.
func (pxgb *PublicationXClassicGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pxgb.build.ctx, "GroupBy")
	if err := pxgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PublicationXClassicQuery, *PublicationXClassicGroupBy](ctx, pxgb.build, pxgb, pxgb.build.inters, v)
}

func (pxgb *PublicationXClassicGroupBy) sqlScan(ctx context.Context, root *PublicationXClassicQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pxgb.fns))
	for _, fn := range pxgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pxgb.flds)+len(pxgb.fns))
		for _, f := range *pxgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pxgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pxgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PublicationXClassicSelect is the builder for selecting fields of PublicationXClassic entities.
type PublicationXClassicSelect struct {
	*PublicationXClassicQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pxs *PublicationXClassicSelect) Aggregate(fns ...AggregateFunc) *PublicationXClassicSelect {
	pxs.fns = append(pxs.fns, fns...)
	return pxs
}

// Scan applies the selector query and scans the result into the given value.
func (pxs *PublicationXClassicSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pxs.ctx, "Select")
	if err := pxs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PublicationXClassicQuery, *PublicationXClassicSelect](ctx, pxs.PublicationXClassicQuery, pxs, pxs.inters, v)
}

func (pxs *PublicationXClassicSelect) sqlScan(ctx context.Context, root *PublicationXClassicQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pxs.fns))
	for _, fn := range pxs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pxs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pxs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
