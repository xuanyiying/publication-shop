// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"github.com/go-kratos/publication-shop/app/catalog/service/internal/data/ent/publicationxclassic"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PublicationXClassic is the model entity for the PublicationXClassic schema.
type PublicationXClassic struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 详情ID
	ClassicID int `json:"classic_id,omitempty"`
	// 国际标准书号
	Isbn string `json:"isbn,omitempty"`
	// 详情
	ClassicName string `json:"classic_name,omitempty"`
	// 出版物ID
	PublicationID int `json:"publication_id,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PublicationXClassic) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case publicationxclassic.FieldID, publicationxclassic.FieldClassicID, publicationxclassic.FieldPublicationID:
			values[i] = new(sql.NullInt64)
		case publicationxclassic.FieldIsbn, publicationxclassic.FieldClassicName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PublicationXClassic fields.
func (px *PublicationXClassic) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case publicationxclassic.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			px.ID = int(value.Int64)
		case publicationxclassic.FieldClassicID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field classic_id", values[i])
			} else if value.Valid {
				px.ClassicID = int(value.Int64)
			}
		case publicationxclassic.FieldIsbn:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field isbn", values[i])
			} else if value.Valid {
				px.Isbn = value.String
			}
		case publicationxclassic.FieldClassicName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field classic_name", values[i])
			} else if value.Valid {
				px.ClassicName = value.String
			}
		case publicationxclassic.FieldPublicationID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field publication_id", values[i])
			} else if value.Valid {
				px.PublicationID = int(value.Int64)
			}
		default:
			px.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PublicationXClassic.
// This includes values selected through modifiers, order, etc.
func (px *PublicationXClassic) Value(name string) (ent.Value, error) {
	return px.selectValues.Get(name)
}

// Update returns a builder for updating this PublicationXClassic.
// Note that you need to call PublicationXClassic.Unwrap() before calling this method if this PublicationXClassic
// was returned from a transaction, and the transaction was committed or rolled back.
func (px *PublicationXClassic) Update() *PublicationXClassicUpdateOne {
	return NewPublicationXClassicClient(px.config).UpdateOne(px)
}

// Unwrap unwraps the PublicationXClassic entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (px *PublicationXClassic) Unwrap() *PublicationXClassic {
	_tx, ok := px.config.driver.(*txDriver)
	if !ok {
		panic("ent: PublicationXClassic is not a transactional entity")
	}
	px.config.driver = _tx.drv
	return px
}

// String implements the fmt.Stringer.
func (px *PublicationXClassic) String() string {
	var builder strings.Builder
	builder.WriteString("PublicationXClassic(")
	builder.WriteString(fmt.Sprintf("id=%v, ", px.ID))
	builder.WriteString("classic_id=")
	builder.WriteString(fmt.Sprintf("%v", px.ClassicID))
	builder.WriteString(", ")
	builder.WriteString("isbn=")
	builder.WriteString(px.Isbn)
	builder.WriteString(", ")
	builder.WriteString("classic_name=")
	builder.WriteString(px.ClassicName)
	builder.WriteString(", ")
	builder.WriteString("publication_id=")
	builder.WriteString(fmt.Sprintf("%v", px.PublicationID))
	builder.WriteByte(')')
	return builder.String()
}

// PublicationXClassics is a parsable slice of PublicationXClassic.
type PublicationXClassics []*PublicationXClassic
